import 'dart:ui';

class AppColor {
  // Class of color constants
  // This class contains the colors that are used in the app

  // Background colors
  static const background = Color(0x00000000);

  // Appbar colors
  static const surface = Color(0xFFFFFFFF);
  static const onSurface = Color(0xFF9BC9E3);

  // Primary and secondary colors
  static const primary = Color(0xFFFEDF00);
  static const onPrimary = Color(0xFFFFFFFF);
  static const secondary = Color(0xFF9BC9E3);
  static const onSecondary = Color(0xFFFFFFFF);

  static const error = Color(0xFFFF0C2C);
  static const onError = Color(0xFFFAFAFA);

  static const iconDark = Color(0xFFFFFFFF);
}
